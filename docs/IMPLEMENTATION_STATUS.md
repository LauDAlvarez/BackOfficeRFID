# Estado de implementación

- **Fase actual:** 7 — Hardening implementado; cierre condicionado a verificación visual y Docker disponibles.
- **Completado:** revisión de permisos, schemas, errores, carga/vacío, accesibilidad y estilos responsive. Referencias/exportaciones rechazan páginas inconsistentes; reintentos de importación con un único aviso accesible; MIME de exportación explícito y `nosniff` conservado en Nginx. Fase 6: importación CSV/XLSX de alumnos/profesores, plantillas, preview, errores y duplicados; confirmación atómica mock y exportación filtrada para ambos roles.
- **Verificado:** `npm ci`, `npm run lint`, `npm run typecheck`, `npm test` (356 pruebas en 27 archivos), `npm run build`, `npm audit --audit-level=low` (0 vulnerabilidades), `git diff --check`; ocho rutas y recursos JS/CSS/XLSX del preview respondieron 200. Testing Library verifica permisos por rol, mutaciones directas, reintentos y navegación móvil/cambio a escritorio.
- **Pendiente:** comprobación visual desktop/tablet/mobile: ningún navegador conectado y Computer Use nativo sin pipe. Docker no está instalado; configuración inspeccionada, sin ejecutar build/contenedor. Código y jsdom no sustituyen esas verificaciones. Build exitoso con aviso de tamaño: principal 557 kB y ExcelJS diferido 930 kB.
- **Decisiones:** cambios puntuales sobre la arquitectura existente, sin dependencias nuevas en Fase 7. ExcelJS de Fase 6 carga bajo demanda; importaciones máximo 2 MB/1000 filas, revalidación completa antes de confirmar. Sin backend ni Raspberry Pi; datos mock únicamente en memoria, sin garantías de seguridad real.
