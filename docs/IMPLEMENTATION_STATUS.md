# Estado de implementación

- **Fase actual:** 5 — Gestión académica y RFID implementada; cierre visual pendiente por limitación del entorno.
- **Completado:** consulta y asociación RFID; asistencia y corrección manual; evaluaciones/resultados; notas 0–10 y aprobación desde 6; acceso a condición académica/estado de cursada; cuotas y situación derivada del alumno. Tablas, filtros, formularios, permisos, bajas lógicas y contratos HTTP sobre mocks. Sin backend, Raspberry Pi ni funcionalidades de Fase 6.
- **Verificado:** lint, typecheck, suite completa de 294 pruebas, build, `git diff --check` y respuesta HTTP de seis rutas del preview. Tras ajustar mensajes de validación en español se repitieron las 22 pruebas de schemas, lint y build/typecheck. Responsive revisado en código y navegación móvil probada con Testing Library.
- **Pendiente:** revisión visual desktop/tablet/mobile: ningún navegador disponible, `Browser is not available: iab` y Computer Use nativo con `native pipe unavailable`. La inspección de código y jsdom no equivalen a una comprobación visual. Build exitoso con aviso de bundle principal de 541 kB. Docker sigue sin verificar desde Foundation; no se modificó infraestructura.
- **Decisiones:** correcciones conservan origen RFID; historial bloquea cambios incompatibles y bajas de referencias. Condición académica explícita, sin promoción automática por nota. Cuotas derivadas de pago/vencimiento en Córdoba; RFID normalizado y único entre personas. Datos solo en memoria y ninguna dependencia nueva del proyecto.
